---
title: "MODEL"
author: "Allison Shi"
date: "`r Sys.Date()"
output: pdf_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r load-packages, warning = F, message = F}
library(tidyverse)
library(knitr)
library(broom)
library(nnet) # for multinomial logistic regression
```

```{r, message = F}
us_19 <- read_csv('~/df_data/US/us_19.csv')
#glimpse(us_19)
```

## EDA

`CAN_USE` = 
```{r}
ggplot(data = us_19, aes(x = factor(DAST_CAT), fill = factor(CAN_USE))) +
  geom_bar()
```

```{r}
us_19 %>%
  count(CAN_USE, DAST_CAT)
```

